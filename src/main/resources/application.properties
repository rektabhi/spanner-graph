# Community Detection System Configuration

# Google Cloud Spanner Configuration
spanner.project.id=${SPANNER_PROJECT_ID:your-project-id}
spanner.instance.id=${SPANNER_INSTANCE_ID:your-instance-id}
spanner.database.id=${SPANNER_DATABASE_ID:community-db}

# Logging Configuration
logging.level.com.sumo=DEBUG
logging.level.com.google.cloud.spanner=INFO

# Application Configuration
app.name=Community Detection System
app.version=1.0.0
